Docker build:-
------------
  *Below command for create and push image to docker hub
  #docker bulid -t url/username/image:version .
  *Below command for dockerfile bulid in local
  #docker build image:version .
  * below command for push the local image to docker registry
  #docker tag image:version url/username/image:version
RUN: Docker RUN command run at time image buliding. It will run like package installation and configuration
